local speed = 50
local rigidBody = nil

function start()
    print("lua start...")
    print("injected object", cube)
    --material = cube:GetComponent(typeof(CS.UnityEngine.Material))
end

function update()
    local r = CS.UnityEngine.Vector3.forward * CS.UnityEngine.Time.deltaTime * speed
    cube.transform:Rotate(r)
    --cube.material.color = CS.UnityEngine.Color(CS.UnityEngine.Mathf.Sin(CS.UnityEngine.Time.time) / 2 + 0.5, 0, 0, 1)
end

function ondestroy()
    print("lua destroy")
end